{
  "name": "messenger",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "test": "cd tests && venv/bin/python -m pytest",
    "install": "concurrently \"npm install --prefix backend\" \"npm install --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "mongodb": "^6.9.0"
  }
}
